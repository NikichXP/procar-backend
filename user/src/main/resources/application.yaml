server:
  port: 8084

spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/procar

logging:
  level:
    com.procar.user: DEBUG
    org.springframework.data.mongodb: DEBUG
